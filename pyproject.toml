[project]
name = "ewd-algorithms"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Herve Rwigema", email = "imenarwigema@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
ewd-algorithms = "ewd_algorithms:main"

[build-system]
requires = ["uv_build>=0.12.18,<0.13.0"]
build-backend = "uv_build"
