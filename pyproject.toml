[project]
name = "ewd-algorithms"
version = "0.1.0"
description = "An experiment comparing three ways to find one driver in a list of 10,000"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[project.scripts]
ewd-algorithms = "ewd_algorithms.main:main"

[build-system]
requires = ["uv_build>=0.12.18,<0.13.0"]
build-backend = "uv_build"
